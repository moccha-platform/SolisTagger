import { settingsParams } from '../../ui/src/app/interfaces/settingsParams.interface'

export interface settingsUpdate {
    search?: settingsParams['search']
    appearance?: settingsParams['appearance']
}